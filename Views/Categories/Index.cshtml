@model IEnumerable<bookstore.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BCategoryID })
        </td>
    </tr>
}

</table>
