@model IEnumerable<bookstore.Models.Book>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BCategoryID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BCategoryID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BId })
        </td>
    </tr>
}

</table>
